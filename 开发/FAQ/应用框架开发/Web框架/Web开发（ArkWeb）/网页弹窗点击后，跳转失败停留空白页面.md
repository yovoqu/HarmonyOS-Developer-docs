# 网页弹窗点击后，跳转失败停留空白页面

更新时间：2026-06-26 09:07:13

来源：https://developer.huawei.com/consumer/cn/doc/harmonyos-faqs/faqs-arkweb-110

## 网页弹窗点击后，跳转失败停留空白页面
 


##### 问题现象

应用网页弹窗点击“取消”后，会停留在一个空白页面。
 
 

##### 背景知识

[backward](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/arkts-apis-webview-webviewcontroller#backward)按照网页历史栈，后退一个页面。一般结合[accessBackward](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/arkts-apis-webview-webviewcontroller#accessbackward)一起使用。
 
 

##### 问题定位

弹窗提示选择“取消”后，弹窗消失，页面保持原有空白页面。同时，侧滑后返回了上一界面。证明网页未进行返回或者跳转新页面来响应“取消”事件。
 
 

##### 分析结论

应用没有对“取消”事件做返回处理。
 
 

##### 修改建议

- 使用backward返回上一页面。简易示例代码如下：
 
```ArkTS
// xxx.ets
import { webview } from '@kit.ArkWeb';
import { BusinessError } from '@kit.BasicServicesKit';


@Entry
@Component
struct WebBackward {
  controller: webview.WebviewController = new webview.WebviewController();

  build() {
    Column() {
      Blank()
        .height(20)
      Button('返回')
        .onClick(() => {
          try {
            this.controller.backward();
          } catch (error) {
            console.error(`ErrorCode: ${(error as BusinessError).code},  Message: ${(error as BusinessError).message}`);
          }
        })
      Web({ src: $rawfile('one.html'), controller: this.controller })
        .fileAccess(false)
        .geolocationAccess(false)
    }
  }
}
```

- 修改网页代码，添加返回上一页面操作。
```text


    
    页面1
    
        body {
            font-family: Arial, sans-serif;
            padding: 20px;
        }
        .nav-link {
            display: inline-block;
            padding: 15px 25px;
            background: #4CAF50;
            color: white;
            text-decoration: none;
            border-radius: 5px;
        }
    


这是页面1
点击下方按钮跳转到页面2：
前往页面2


```
 
```text


    
    页面2
    
        body {
            font-family: Arial, sans-serif;
            padding: 20px;
        }
        .back-btn {
            display: inline-block;
            padding: 15px 25px;
            background: #f44336;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            margin-bottom: 20px;
        }
    
    
        // 移动端侧滑返回功能
        let touchStartX = 0;
        let touchStartY = 0;

        document.addEventListener('touchstart', function(e) {
            touchStartX = e.changedTouches[0].screenX;
            touchStartY = e.changedTouches[0].screenY;
        });

        document.addEventListener('touchend', function(e) {
            const touchEndX = e.changedTouches[0].screenX;
            const touchEndY = e.changedTouches[0].screenY;

            // 右滑返回
            if (touchEndX > touchStartX + 30 && Math.abs(touchEndY - touchStartY)    // 返回上一级页面
            history.back();
        }

        function historyGo() {
            // 返回上一级页面
            history.go(-1);
        }

        function windowHistoryGo() {
            // 返回上一级页面
            window.history.go(-1);
        }

        function windowHistoryBack() {
            // 返回上一级页面
            window.history.back();
        }
    


这是页面2
点击下方按钮或者右滑跳转到页面2：
historyBack返回页面1
historyGo返回页面1
windowHistoryGo返回页面1
windowHistoryBack返回页面1


```
