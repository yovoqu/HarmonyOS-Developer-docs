# 如何实现应用退出到后台时新增蒙层保护隐私

更新时间：2026-06-26 07:47:42

来源：https://developer.huawei.com/consumer/cn/doc/harmonyos-faqs/faqs-arkui-838

## 如何实现应用退出到后台时新增蒙层保护隐私
 


##### 问题现象

当应用进入后台，为了保护隐私，会给应用界面增加一个遮罩，例如其他平台上的银行应用切到后台后，任务列表中显示的应用界面增加了遮罩。
 
 

##### 效果预览


![](https://contentcenter-vali-drcn.dbankcdn.cn/pvt_2/DeveloperAlliance_scene_100_1/7d/v3/uUVJuwsHSM6XED_hWmd1fQ/zh-cn_image_0000002628558544.png?HW-CC-KV=V1&HW-CC-Date=20260701T025628Z&HW-CC-Expire=86400&HW-CC-Sign=1C61C03273DEBE4AC07A6745476E9634E50BDA4A9489DE88FC5499DFA544D49B)
 
![](https://contentcenter-vali-drcn.dbankcdn.cn/pvt_2/DeveloperAlliance_scene_100_1/c7/v3/2c9zFsxhR1WGwxD-qIzTjw/zh-cn_image_0000002658917855.png?HW-CC-KV=V1&HW-CC-Date=20260701T025628Z&HW-CC-Expire=86400&HW-CC-Sign=A5C462B4624AEAC2B3DE2B1EC49483BD27D7EDBE27319F5F66384A03716EF5F2)

 
 

##### 背景知识

- [@ohos.window (窗口)](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/js-apis-window)：窗口提供管理窗口的一些基础能力，包括对当前窗口的创建、销毁、各属性设置，以及对各窗口间的管理调度。
- 组件内容模糊：[foregroundBlurStyle](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/ts-universal-attributes-foreground-blur-style#foregroundblurstyle)。

 
 

##### 解决方案

实现思路：
 
- 需要判断应用当前在前台/后台，可查看[如何判断应用当前在前台/后台](https://developer.huawei.com/consumer/cn/doc/harmonyos-faqs/faqs-ability-101)；
- 需开启windowStage生命周期变化的监听：[on('windowStageEvent')](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/arkts-apis-window-windowstage#onwindowstageevent9)。

 
具体方案：
 
- 在EntryAbility.ets文件中获取windowStage并在应用全局存储。
```text
import { ConfigurationConstant, UIAbility } from '@kit.AbilityKit';
import { hilog } from '@kit.PerformanceAnalysisKit';
import { window } from '@kit.ArkUI';

const DOMAIN = 0x0000;

export default class EntryAbility extends UIAbility {
  onCreate(): void {
    try {
      this.context.getApplicationContext().setColorMode(ConfigurationConstant.ColorMode.COLOR_MODE_NOT_SET);
    } catch (err) {
      hilog.error(DOMAIN, 'testTag', 'Failed to set colorMode. Cause: %{public}s', JSON.stringify(err));
    }
    hilog.info(DOMAIN, 'testTag', '%{public}s', 'Ability onCreate');
  }

  onDestroy(): void {
    hilog.info(DOMAIN, 'testTag', '%{public}s', 'Ability onDestroy');
  }

  onWindowStageCreate(windowStage: window.WindowStage): void {
    // Main window is created, set main page for this ability
    hilog.info(DOMAIN, 'testTag', '%{public}s', 'Ability onWindowStageCreate');

    // 全局存储windowStage
    AppStorage.setOrCreate('windowStage', windowStage);
    windowStage.loadContent('pages/Index', (err) => {
      if (err.code) {
        hilog.error(DOMAIN, 'testTag', 'Failed to load the content. Cause: %{public}s', JSON.stringify(err));
        return;
      }
      hilog.info(DOMAIN, 'testTag', 'Succeeded in loading the content.');
    });
  }

  onWindowStageDestroy(): void {
    // Main window is destroyed, release UI related resources
    hilog.info(DOMAIN, 'testTag', '%{public}s', 'Ability onWindowStageDestroy');
  }

  onForeground(): void {
    // Ability has brought to foreground
    hilog.info(DOMAIN, 'testTag', '%{public}s', 'Ability onForeground');
  }

  onBackground(): void {
    // Ability has back to background
    hilog.info(DOMAIN, 'testTag', '%{public}s', 'Ability onBackground');
  }
};
```

- Index.ets实现应用状态监听以及组件模糊。
```text
import { window } from '@kit.ArkUI';

@Entry
@Component
struct Index {
  message: string = 'Hello World';
  @State flag: boolean = false;

  onPageShow(): void {
    let windowStage = AppStorage.get('windowStage') as window.WindowStage;
    // 监听windowStageEvent事件，判断应用是否处于前台不可交互状态
    windowStage.on('windowStageEvent', (data) => {
      if (data === window.WindowStageEventType.PAUSED) {
        this.flag = true;
      } else {
        this.flag = false;
      }
    });
  }

  build() {
    Row() {
      Column() {
        Text(this.message)
          .fontSize(50)
          .fontWeight(FontWeight.Bold);
      }
      .width('100%');
    }
    .height('100%')
    // 如果处于前台不可交互状态，为当前组件提供内容模糊能力，否则关闭模糊
    .foregroundBlurStyle(this.flag ? BlurStyle.Thin : BlurStyle.NONE, {
      colorMode: ThemeColorMode.LIGHT,
      adaptiveColor: AdaptiveColor.DEFAULT
    });
  }
}
```


 
 

##### 常见FAQ

Q：应用退至多任务列表时，如何全局设置蒙层隐私效果，不用每个页面都设置一次？
 
A：可以使用Navigation路由跳转方案，只需在Navigation根容器页面监听windowStageEvent事件并设置foregroundBlurStyle属性即可，子页面无需设置也会有蒙层隐私效果。
 
Q：应用退至多任务列表时，自定义键盘没有蒙层隐私效果。
 
A：需要在自定义键盘组件中也使用foregroundBlurStyle属性设置。如果自定义键盘组件是@Component修饰的，另外还需要在aboutToAppear()中监听windowStageEvent事件。
 
 

##### 总结

想要实现应用退出到后台时保护隐私:
 
- 首先考虑当前应用退出到后台的生命周期如何监听。
- 实现保护效果可以使用页面组件模糊或者还可以新增自定义蒙层。
