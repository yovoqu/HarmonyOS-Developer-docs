# 点击文字在文字中心显示标记图案

更新时间：2026-06-26 09:07:13

来源：https://developer.huawei.com/consumer/cn/doc/harmonyos-faqs/faqs-arkui-1456

#### 问题现象

如何实现点击某个组件（如Text），在组件中心显示一个标记？
 
 

#### 效果预览

点击文字“餐”后，Circle组件会移动至餐的中心并显示出来
 

![](https://contentcenter-vali-drcn.dbankcdn.cn/pvt_2/DeveloperAlliance_scene_100_1/38/v3/KevjSfNpTwWWNsSgUpobvA/zh-cn_image_0000002628604266.gif?HW-CC-KV=V1&HW-CC-Date=20260701T041222Z&HW-CC-Expire=86400&HW-CC-Sign=472A4B0E474BD67FD3225477DB35C84CCBD89A65BFB8F10DA407FA6F79F9BA2D)

 
 

#### 背景知识

- [ComponentUtils](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/arkts-apis-uicontext-componentutils)对象提供获取组件绘制区域坐标和大小的能力。[getRectangleById](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/arkts-apis-uicontext-componentutils#getrectanglebyid)方法根据组件ID获取组件实例对象，可以通过组件实例对象获取的坐标位置和大小。
- [visibility](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/ts-universal-attributes-visibility#visibility)控制组件的显示或隐藏。当未设置visibility时，组件默认为显示。
- [hitTestBehavior](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/ts-universal-attributes-hit-test-behavior#hittestbehavior)设置组件的触摸测试类型。入参设置为HitTestMode.Transparent时，自身不响应触摸测试，兄弟节点和祖先节点可以正常响应。

 
 

#### 解决方案

使用Stack包裹Text组件和点击事件后的Circle组件，设置子组件在容器内的对齐方式为横向和纵向居中，计算两中心坐标的差值，使用offset根据偏移量移动Circle组件到目标组件的中心点。
 
```text
import { componentUtils } from '@kit.ArkUI';

@Entry
@Component
struct ImageDemo {
  @State xText: number = 0;
  @State yText: number = 0;
  @State xCircle: number = 0;
  @State yCircle: number = 0;
  @State flag: boolean = false;

  build() {
    Column() {
      Stack() {
        Text('餐')
          .fontSize(100)
          .fontWeight(FontWeight.Bold)
          .textAlign(TextAlign.Center)
          .id('test')
          .onClick(() => {
            let textPosition: componentUtils.ComponentInfo =
              this.getUIContext().getComponentUtils().getRectangleById('test');
            let circlePosition: componentUtils.ComponentInfo =
              this.getUIContext().getComponentUtils().getRectangleById('circle');
            let width = textPosition.size.width;
            let height = textPosition.size.height;
           <em> // 相对于屏幕的坐标</em>
            let x1 = textPosition.screenOffset.x;
            let y1 = textPosition.screenOffset.y;
            <em>// 组件中心点相对于屏幕的坐标</em>
            this.xText = x1 + width / 2;
            this.yText = y1 + height / 2;

          <em>  // circle相较于屏幕的坐标</em>
            let x2 = circlePosition.screenOffset.x;
            let y2 = circlePosition.screenOffset.y;
           <em> // circle组件的宽高</em>
            let width2 = circlePosition.size.width;
            let height2 = circlePosition.size.height;
          <em>  // circle组件的中心坐标</em>
            this.xCircle = x2 + width2 / 2;
            this.yCircle = y2 + height2 / 2;

            this.flag = !this.flag;
          });
        Circle()
          .width(50)
          .height(50)
          .fill('#0A59F7')
          .id('circle')
          .offset({
            x: this.getUIContext().px2vp(this.xText - this.xCircle),
            y: this.getUIContext().px2vp(this.yText - this.yCircle)
          })
          .visibility(this.flag ? Visibility.Visible : Visibility.None)
          .hitTestBehavior(HitTestMode.Transparent);
      };
    }
    .width('100%')
    .height('100%')
    .justifyContent(FlexAlign.Center);
  }
}
```
 
 

#### 总结

控制组件显隐visibility属性和组件位置offset属性搭配使用，同时使用Stack布局，以实现点击组件显示覆盖标记的效果。上述效果为标记在点击组件中心显隐，开发者也可基于此实现手指点击位置显隐的效果。
