# 页面没有实现护眼模式

更新时间：2026-06-26 09:07:13

来源：https://developer.huawei.com/consumer/cn/doc/harmonyos-faqs/faqs-arkui-861

#### 问题现象

应用阅读页面未实现护眼模式。
 
 

#### 背景知识

护眼模式可通过以下方式实现：
 
- 通过设置根容器的[grayscale](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/ts-universal-attributes-image-effect#grayscale)，[saturate](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/ts-universal-attributes-image-effect#saturate)等属性，实现护眼模式。因页面和组件没有继承机制，暂时无法实现全局生效，只能每个页面单独去实现。
- 使用[createSubWindow](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/arkts-apis-window-windowstage#createsubwindow9)创建子窗口，使用[setUIContent](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/arkts-apis-window-window#setuicontent9)根据当前工程中指定的某个页面路径为窗口加载具体页面内容，实现护眼模式。

 
 

#### 问题定位

查看应用的相关设置，全局搜索grayscale+saturate，createSubWindow+setUIContent，排查应用是否设置了护眼模式。
 
 

#### 分析结论

应用未使用grayscale+saturate或createSubWindow+setUIContent设置护眼模式。
 
 

#### 修改建议

- 方案一：通过设置根容器的grayscale，saturate等属性，实现护眼模式。设置页面属性：
```text
<em>// 通过grayscale和saturate属性实现护眼模式。</em>
.grayscale(this.grayScaleValue)<em> // 设置灰度值，可调整页面亮度</em>
.saturate(this.saturateValue) <em>// 设置颜色饱和度</em>
.backgroundColor(this.backgroundColorValue); <em>// 设置页面颜色</em>
```
 开启/关闭护眼模式时改变grayscale，saturate等属性的值：

  
```text
Column() {
  Button('开启护眼模式')
    .onClick(() => {
      this.grayScaleValue = 0.1;
      this.saturateValue = 0.72;
      this.backgroundColorValue = '#33eee685'; <em>// 设置页面颜色淡黄</em>
    });

  Button('关闭护眼模式')
    .onClick(() => {
      this.grayScaleValue = 0;
      this.saturateValue = 1;
      this.backgroundColorValue = ''; <em>// 设置页面颜色透明</em>
    })
    .margin({ top: 10 });
}
.margin({ left: 20 });
```
 效果图如下：

  
![](https://contentcenter-vali-drcn.dbankcdn.cn/pvt_2/DeveloperAlliance_scene_100_1/33/v3/A4X7be_UQa2NRqvtM5GobQ/zh-cn_image_0000002628558792.png?HW-CC-KV=V1&HW-CC-Date=20260730T072430Z&HW-CC-Expire=86400&HW-CC-Sign=2F06A54122281636FB7FBDE2EA7B85F4A982ABAF0A2AAFB25F01A1B2813CEDBC)


 
- 方案二：使用createSubWindow和setUIContent实现护眼模式。
```text
Column() {

  Button('开启护眼模式')
    .onClick(() => {
      if (!this.showWindow) {
       <em> // 通过不同颜色的子窗口覆盖在主窗口上实现护眼模式</em>
        EyeProtectionMode.getInstance().createSubWithEyeWindow('#1Af3f364');
        this.showWindow = true;
      }
    })

  Button('关闭护眼模式')
    .onClick(() => {
      if (this.showWindow) {
        <em>// 移除窗口</em>
        EyeProtectionMode.getInstance().removeSubWithEyeWindow();
        this.showWindow = false;
      }
    })
    .margin({ top: 10 })
}
.margin({ left: 20 })
```
 效果图如下：

  
![](https://contentcenter-vali-drcn.dbankcdn.cn/pvt_2/DeveloperAlliance_scene_100_1/5c/v3/-YTYXLRVTF6LzYqkU93zZA/zh-cn_image_0000002658918109.png?HW-CC-KV=V1&HW-CC-Date=20260730T072430Z&HW-CC-Expire=86400&HW-CC-Sign=7CF3948D752CC10000E77FC761F65A7AC2A27605A6F38C99376AEF26AB180B1E)


 
完整示例：
 
```text
import { EyeProtectionMode } from '../utils/EyeProtectionMode';
import { window } from '@kit.ArkUI';
import { common } from '@kit.AbilityKit';

@Entry
@Component
struct Index {
  private message: string =
    '永和九年，岁在癸丑，暮春之初，会于会稽山阴之兰亭，修禊事也。群贤毕至，少长咸集。此地有崇山峻岭，茂林修竹；又有清流激湍，映带左右，引以为流觞曲水，列坐其次。虽无丝竹管弦之盛，一觞一咏，亦足以畅叙幽情。\n' +
      '  是日也，天朗气清，惠风和畅。仰观宇宙之大，俯察品类之盛，所以游目骋怀，足以极视听之娱，信可乐也。\n' +
      '  夫人之相与，俯仰一世，或取诸怀抱，悟言一室之内；或因寄所托，放浪形骸之外。虽趣舍万殊，静躁不同，当其欣于所遇，暂得于己，快然自足，不知老之将至。及其所之既倦，情随事迁，感慨系之矣。向之所欣，俯仰之间，已为陈迹，犹不能不以之兴怀。况修短随化，终期于尽。古人云：“死生亦大矣。”岂不痛哉！' +
      '永和九年，岁在癸丑，暮春之初，会于会稽山阴之兰亭，修禊事也。群贤毕至，少长咸集。此地有崇山峻岭，茂林修竹；又有清流激湍，映带左右，引以为流觞曲水，列坐其次。虽无丝竹管弦之盛，一觞一咏，亦足以畅叙幽情。\\n\' +\n' +
      '    \'  是日也，天朗气清，惠风和畅。仰观宇宙之大，俯察品类之盛，所以游目骋怀，足以极视听之娱，信可乐也。\\n\' +\n' +
      '    \'  夫人之相与，俯仰一世，或取诸怀抱，悟言一室之内；或因寄所托，放浪形骸之外。虽趣舍万殊，静躁不同，当其欣于所遇，暂得于己，快然自足，不知老之将至。及其所之既倦，情随事迁，感慨系之矣。向之所欣，俯仰之间，已为陈迹，犹不能不以之兴怀。况修短随化，终期于尽。古人云：“死生亦大矣。”岂不痛哉！' +
      '永和九年，岁在癸丑，暮春之初，会于会稽山阴之兰亭，修禊事也。群贤毕至，少长咸集。此地有崇山峻岭，茂林修竹；又有清流激湍，映带左右，引以为流觞曲水，列坐其次。虽无丝竹管弦之盛，一觞一咏，亦足以畅叙幽情。\\n\' +\n' +
      '    \'  是日也，天朗气清，惠风和畅。仰观宇宙之大，俯察品类之盛，所以游目骋怀，足以极视听之娱，信可乐也。\\n\' +\n' +
      '    \'  夫人之相与，俯仰一世，或取诸怀抱，悟言一室之内；或因寄所托，放浪形骸之外。虽趣舍万殊，静躁不同，当其欣于所遇，暂得于己，快然自足，不知老之将至。及其所之既倦，情随事迁，感慨系之矣。向之所欣，俯仰之间，已为陈迹，犹不能不以之兴怀。况修短随化，终期于尽。古人云：“死生亦大矣。”岂不痛哉！' +
      '永和九年，岁在癸丑，暮春之初，会于会稽山阴之兰亭，修禊事也。群贤毕至，少长咸集。此地有崇山峻岭，茂林修竹；又有清流激湍，映带左右，引以为流觞曲水，列坐其次。虽无丝竹管弦之盛，一觞一咏，亦足以畅叙幽情。\\n\' +\n' +
      '    \'  是日也，天朗气清，惠风和畅。仰观宇宙之大，俯察品类之盛，所以游目骋怀，足以极视听之娱，信可乐也。\\n\' +\n' +
      '    \'  夫人之相与，俯仰一世，或取诸怀抱，悟言一室之内；或因寄所托，放浪形骸之外。虽趣舍万殊，静躁不同，当其欣于所遇，暂得于己，快然自足，不知老之将至。及其所之既倦，情随事迁，感慨系之矣。向之所欣，俯仰之间，已为陈迹，犹不能不以之兴怀。况修短随化，终期于尽。古人云：“死生亦大矣。”岂不痛哉！';
  @State grayScaleValue: number = 0;
  @State saturateValue: number = 1;
  @State backgroundColorValue: string = '';
  context: common.UIAbilityContext = this.getUIContext().getHostContext() as common.UIAbilityContext;
  private showWindow: boolean = false;

  aboutToAppear(): void {
    window.getLastWindow(this.context).then((lastWindow) => {
      lastWindow.setWindowLayoutFullScreen(true);
    });
  }

  build() {
    Column() {
      Scroll() {
        Text(this.message)
          .fontSize(20)
          .width('90%');
      }
      .height('70%')
      .width('100%');

      Row() {
        Text('使用grayscale+saturate：')
          .width(160);
        Column() {
          Button('开启护眼模式')
            .onClick(() => {
              this.grayScaleValue = 0.1;
              this.saturateValue = 0.72;
              this.backgroundColorValue = '#33eee685'; <em>// 设置页面颜色淡黄</em>
            });

          Button('关闭护眼模式')
            .onClick(() => {
              this.grayScaleValue = 0;
              this.saturateValue = 1;
              this.backgroundColorValue = ''; <em>// 设置页面颜色透明</em>
            })
            .margin({ top: 10 });
        }
        .margin({ left: 20 });

      }
      .margin({ top: 10 })
      .width('90%');

      Row() {
        Text('使用createSubWindow和setUIContent：')
          .width(160)
        Column() {

          Button('开启护眼模式')
            .onClick(() => {
              if (!this.showWindow) {
                <em>// 通过不同颜色的子窗口覆盖在主窗口上实现护眼模式</em>
                EyeProtectionMode.getInstance().createSubWithEyeWindow('#1Af3f364');
                this.showWindow = true;
              }
            })

          Button('关闭护眼模式')
            .onClick(() => {
              if (this.showWindow) {
               <em> // 移除窗口</em>
                EyeProtectionMode.getInstance().removeSubWithEyeWindow();
                this.showWindow = false;
              }
            })
            .margin({ top: 10 })
        }
        .margin({ left: 20 })
      }
      .margin({ top: 20 })
      .width('90%')
    }
    .height('100%')
    .padding({ top: '123px', bottom: '91px' })
    .width('100%')
   <em> // 通过grayscale和saturate属性实现护眼模式。</em>
    .grayscale(this.grayScaleValue)<em> // 设置灰度值，可调整页面亮度</em>
    .saturate(this.saturateValue) <em>// 设置颜色饱和度</em>
    .backgroundColor(this.backgroundColorValue); <em>// 设置页面颜色</em>
  }
}
```
 
ets/utils/EyeProtectionMode.ets：
 
```ArkTS
import { window } from '@kit.ArkUI';
import { bundleManager } from '@kit.AbilityKit';
import { BusinessError } from '@kit.BasicServicesKit';

export class EyeProtectionMode {
  windowStage: window.WindowStage | null = null;
  eyeWindowClass: window.Window | null = null;
  private static sInstance: EyeProtectionMode;

  public static getInstance(): EyeProtectionMode {
    if (!EyeProtectionMode.sInstance) {
      EyeProtectionMode.sInstance = new EyeProtectionMode();
    }
    return EyeProtectionMode.sInstance;
  }

  createSubWithEyeWindow(backgroundColor: string): void {
    if (this.windowStage == null) {
      console.error('windowStage为空，请先传值后再创建');
    } else {
     <em> // 创建子窗口</em>
      this.windowStage.createSubWindow('eyeWindow', (error: BusinessError, data) => {
        const errCode: number = error.code;
        if (errCode) {
          console.error(`Failed to create the subwindow. Cause code: ${error.code}, message: ${error.message}`);
          return;
        }
        <em>// 1、单列设置的属性</em>
        this.eyeWindowClass = data;
        <em>// 2、设置全屏显示</em>
        this.eyeWindowClass?.setWindowLayoutFullScreen(true);
        <em>// 3、设置不可触摸，防止护眼模式窗口阻挡主窗口的手势</em>
        this.eyeWindowClass.setWindowTouchable(false); 
        <em>// 4、设置window的承载content，必须设置，否则会显示失败</em>
        <em>// 下面是页面路由路径的获取，appBundleName方法已在下面贴出，TBXBaseLib是我自己的具体组件模块名：</em>
        const pagePath = 'ets/utils/EyesProtectPage.ets';
        let url = `@bundle:${this.appBundleName()}/entry/${pagePath}`;
        this.eyeWindowClass.setUIContent(url, () => {
         <em> // 5.设置背景颜色</em>
          this.eyeWindowClass?.setWindowBackgroundColor(backgroundColor);
        });

       <em> // 6、显示窗口</em>
        this.eyeWindowClass?.showWindow(() => {
        });
      });
    }
  }

  <em>// 移除护眼模式子窗口</em>
  removeSubWithEyeWindow(): void {
    if (this.eyeWindowClass != null) {
      this.eyeWindowClass.destroyWindow(() => {
        this.eyeWindowClass = null;
      });
    }

  }

  <em>// 获取app的bundleName</em>
  appBundleName(): string {
    return bundleManager.getBundleInfoForSelfSync(bundleManager.BundleFlag.GET_BUNDLE_INFO_WITH_APPLICATION)
      .appInfo
      .descriptionResource
      .bundleName;
  }
}
```
 
src/main/ets/entryability/EntryAbility.ets：
 
```json
import { ConfigurationConstant, UIAbility } from '@kit.AbilityKit';
import { hilog } from '@kit.PerformanceAnalysisKit';
import { window } from '@kit.ArkUI';
import { EyeProtectionMode } from '../utils/EyeProtectionMode';

const DOMAIN = 0x0000;

export default class EntryAbility extends UIAbility {
  onCreate(): void {
    try {
      this.context.getApplicationContext().setColorMode(ConfigurationConstant.ColorMode.COLOR_MODE_NOT_SET);
    } catch (err) {
      hilog.error(DOMAIN, 'testTag', 'Failed to set colorMode. Cause: %{public}s', JSON.stringify(err));
    }
    hilog.info(DOMAIN, 'testTag', '%{public}s', 'Ability onCreate');
  }

  onDestroy(): void {
    hilog.info(DOMAIN, 'testTag', '%{public}s', 'Ability onDestroy');
  }

  onWindowStageCreate(windowStage: window.WindowStage): void {
   <em> // Main window is created, set main page for this ability</em>
    hilog.info(DOMAIN, 'testTag', '%{public}s', 'Ability onWindowStageCreate');

    EyeProtectionMode.getInstance().windowStage = windowStage;

    windowStage.loadContent('pages/Index', (err) => {
      if (err.code) {
        hilog.error(DOMAIN, 'testTag', 'Failed to load the content. Cause: %{public}s', JSON.stringify(err));
        return;
      }
      hilog.info(DOMAIN, 'testTag', 'Succeeded in loading the content.');
    });
  }

  onWindowStageDestroy(): void {
   <em> // Main window is destroyed, release UI related resources</em>
    hilog.info(DOMAIN, 'testTag', '%{public}s', 'Ability onWindowStageDestroy');
  }

  onForeground(): void {
    <em>// Ability has brought to foreground</em>
    hilog.info(DOMAIN, 'testTag', '%{public}s', 'Ability onForeground');
  }

  onBackground(): void {
    <em>// Ability has back to background</em>
    hilog.info(DOMAIN, 'testTag', '%{public}s', 'Ability onBackground');
  }
};
```
