# 如何实现随机生成数字图形验证码

更新时间：2026-06-26 09:07:13

来源：https://developer.huawei.com/consumer/cn/doc/harmonyos-faqs/faqs-arkui-1624

#### 问题现象

如何实现下图的验证码功能？
 

![](https://contentcenter-vali-drcn.dbankcdn.cn/pvt_2/DeveloperAlliance_scene_100_1/dc/v3/-57_AQM6QvStUNnP9EwoCQ/zh-cn_image_0000002658976785.png?HW-CC-KV=V1&HW-CC-Date=20260730T072504Z&HW-CC-Expire=86400&HW-CC-Sign=4180AFEE9F78D3D6BAD706979B9930B74A3C43E89B4434D112F0606B1090CFDD)

 
 

#### 背景知识

- [CanvasRenderingContext2D](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/ts-canvasrenderingcontext2d)：使用RenderingContext在Canvas组件上进行绘制，绘制对象可以是矩形、文本、图片等。
- [translate](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/ts-canvasrenderingcontext2d#translate)：移动当前坐标系的原点。
- [rotate](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/ts-canvasrenderingcontext2d#rotate)：针对当前坐标轴进行顺时针旋转。
- [fillText](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/ts-canvasrenderingcontext2d#filltext)：绘制填充类文本。
- [moveTo](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/ts-canvasrenderingcontext2d#moveto)：路径从当前点移动到指定点。
- [lineTo](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/ts-canvasrenderingcontext2d#lineto)：从当前点到指定点进行路径连接。

 
 

#### 解决方案

实现思路如下：
 1. 创建一个自定义的画图形验证码的方法drawImgCode。图形分为3部分：随机数字、随机颜色的线条、随机颜色的点。
2. 封装一个自定义组件ImageCode实现图形验证码。onReady回调里初始化图形验证码，onClick事件刷新图形验证码。
3. 使用TextInput和ImageCode实现图中功能。
 
完整示例参考如下：
 
```text
import { cryptoFramework } from '@kit.CryptoArchitectureKit';

@Entry
@Component
struct ValidationCodeL {
  @State showCode: string = '';

  build() {
    Row() {
      TextInput({ placeholder: '请输入验证码' })
        .layoutWeight(1);
      ImageCode({ showCode: this.showCode, controller: childRef })
        .width(140)
        .height(40);

      Text('刷新').onClick(() => {
        childRef.changeText('');
      });
    }
    .height('100%')
    .width('100%')
    .padding({ left: 30, right: 30 });
  }
}

export class CommonUtil {
<em>  // 随机生成文本内容时使用的值</em>
  sCode: string = '1,2,3,4,5,6,7,8,9,0';
  aCode: string[] = this.sCode.split(',');
  aLength: number = this.aCode.length; <em>// 获取到数组的长度</em>

  static getRandomNumber() {

    let rand = cryptoFramework.createRandom();
  <em>  // 设置生成随机数的字节长度为1</em>
    let randData = rand.generateRandomSync(1);
  <em>  // 自定义范围(0-10之内)</em>
    let numRan: number = Math.fround(randData.data[0] * 10 / 2550);

    return numRan;
  }

<em>  // 绘制图形验证码</em>
  drawImgCode(context: CanvasRenderingContext2D, canvasWidth: number = 100, canvasHeight: number = 40): string {
  <em>  // 用于保存验证码</em>
    let showCode: string = '';
   <em> // 清除当前画布内容，用作刷新画布</em>
    context.clearRect(0, 0, canvasWidth, canvasHeight);

    for (let i = 0; i < 4; i++) {<em> // 这里的for循环可以控制验证码位数</em>
      const j = Math.floor(CommonUtil.getRandomNumber() * this.aLength);<em> // 获取到随机的索引值</em>
      const deg = CommonUtil.getRandomNumber() - 0.5;<em> // 产生一个随机弧度</em>
      const txt = this.aCode[j]; <em>// 得到随机的一个内容</em>
      showCode += txt.toLowerCase(); <em>// 转小写</em>
      const x = 10 + i * 20; <em>// 文字在canvas上的x坐标</em>
      const y = canvasHeight / 2 + CommonUtil.getRandomNumber() * 8; <em>// 文字在canvas上的y坐标</em>
      context.font = '20vp sans-serif';
      context.translate(x, y);
      context.rotate(deg);
      context.fillStyle = this.getColor();
      context.fillText(txt, 0, 0);
      context.rotate(-deg);
      context.translate(-x, -y);
    }
    for (let i = 0; i <= 5; i++) {<em> // 验证码上显示线条</em>
      context.strokeStyle = this.getColor();
      context.beginPath();
      context.moveTo(CommonUtil.getRandomNumber() * canvasWidth, CommonUtil.getRandomNumber() * canvasHeight);
      context.lineTo(CommonUtil.getRandomNumber() * canvasWidth, CommonUtil.getRandomNumber() * canvasHeight);
      context.stroke();
    }
    for (let i = 0; i <= 20; i++) { <em>// 验证码上的小点</em>
      context.strokeStyle = this.getColor(); <em>// 随机生成</em>
      context.beginPath();
      const x = CommonUtil.getRandomNumber() * canvasWidth;
      const y = CommonUtil.getRandomNumber() * canvasHeight;
      context.moveTo(x, y);
      context.lineTo(x + 1, y + 1);
      context.stroke();
    }
    return showCode;
  }

 <em> // 获取随机颜色rgb</em>
  getColor() {
    const r = Math.floor(CommonUtil.getRandomNumber() * 256);
    const g = Math.floor(CommonUtil.getRandomNumber() * 256);
    const b = Math.floor(CommonUtil.getRandomNumber() * 256);
    return 'rgb(' + r + ',' + g + ',' + b + ')';
  }
}

@Component
export default struct ImageCode {
  private settings: RenderingContextSettings = new RenderingContextSettings(true);
  private context: CanvasRenderingContext2D = new CanvasRenderingContext2D(this.settings);
  @State canvasWidth: number = 100;
  @State canvasHeight: number = 40;
  @Link showCode: string;
  private controller: ImageCodeController = new ImageCodeController();

  aboutToAppear(): void {
    if (this.controller) {
      this.controller.changeText = this.changeText;
    }
  }

  private changeText = () => {
    return new CommonUtil().drawImgCode(this.context, this.canvasWidth, this.canvasHeight);
  };

  build() {
    Row() {
  <em>    // 在canvas中调用CanvasRenderingContext2D对象。</em>
      Canvas(this.context)
        .width(this.canvasWidth)
        .height(this.canvasHeight)
        .backgroundColor('#CCC')
        .onReady(() => {
       <em>   // 在这里绘制图形</em>
          this.showCode = new CommonUtil().drawImgCode(this.context, this.canvasWidth, this.canvasHeight);
        })
        .onClick(() => {
          new CommonUtil().drawImgCode(this.context, this.canvasWidth, this.canvasHeight);
        });
    }
    .width('100%')
    .height('100%');
  }
}

class ImageCodeController {
  changeText = (value: string) => {
    return value;
  };
}

let childRef = new ImageCodeController();
```
