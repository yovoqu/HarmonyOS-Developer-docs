# 点击点赞按钮，动画播放完毕后点赞图标消失

更新时间：2026-06-26 09:07:13

来源：https://developer.huawei.com/consumer/cn/doc/harmonyos-faqs/faqs-arkui-882

#### 问题信息

打开页面，正常显示点赞按钮，再点击按钮，播放完点赞动画后，点赞图片消失。
 
 

#### 背景知识

- [if/else条件渲染](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/arkts-rendering-control-ifelse)：条件渲染可根据应用状态，使用if、else和else if渲染相应的UI内容。
- [setTimeout定时器](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/js-apis-timer#settimeout)：设置一个定时器，该定时器在定时器到期后执行一个函数。

 
 

#### 问题定位
1. 使用DevEco Testing的UIViewer工具分别检查点赞按钮正常显示时和消失时的UI结构。确认点赞图标是Image组件，在点赞动画播放完毕后页面上没有Image组件。
![](https://contentcenter-vali-drcn.dbankcdn.cn/pvt_2/DeveloperAlliance_scene_100_1/17/v3/i-DesQy6Rm-EhSFCYVmOJg/zh-cn_image_0000002628559542.png?HW-CC-KV=V1&HW-CC-Date=20260701T041222Z&HW-CC-Expire=86400&HW-CC-Sign=D05B8A8204F8CE0D4EF4F27B88FEFE653A17ED321089E123C4A7F8C26A34C9E1)


  
![](https://contentcenter-vali-drcn.dbankcdn.cn/pvt_2/DeveloperAlliance_scene_100_1/2a/v3/uBosGV4GTdmQ105sqmmlvA/zh-cn_image_0000002658918853.png?HW-CC-KV=V1&HW-CC-Date=20260701T041222Z&HW-CC-Expire=86400&HW-CC-Sign=C1EB0D1D71C4DDAE7D5A55BCFAD24784167350121539745267F10C8BA2585F02)

2. 排查代码中点赞图片的显隐逻辑。点赞图片使用的Image组件通过if/else条件渲染，当变量满足条件时才会正常显示，点赞/取消点赞动画播放完毕后并未修改变量的值，Image组件没有正常渲染。
```text
@Entry
@Component
struct ErrorCode {
  @State isLike: boolean = false; <em>// 点赞状态</em>
  @State clickLikeStatus: boolean | undefined = undefined; <em>// 根据布尔值判断使用取消点赞/点赞动画</em>
  <em>// 点赞图片，请使用尺寸较小的本地图片</em>
  likeIcon: ResourceStr = $r('app.media.ic_public_thumbs_up');
 <em> // 未点赞时的颜色(灰色)</em>
  unlikeColor: string = '#757575';
  <em>// 点赞后的颜色(蓝色)</em>
  likeColor: string = '#4eabe6';

  build() {
    Column() {
      Row() {
        Stack() {
          if (this.clickLikeStatus === undefined) {
            Image(this.likeIcon)
              .objectFit(ImageFit.Contain) <em>// 缩放填充</em>
              .aspectRatio(1) <em>// 固定宽高比为1</em>
              .width(70)
              .syncLoad(true) <em>// 同步加载，避免闪烁</em>
              .fillColor(this.isLike ? this.likeColor : this.unlikeColor)
          } else {
            <em>// 自定义组件，用于播放动画</em>
            LikePlayer({ isLikeAnimation: this.clickLikeStatus })
          }
        }
      }
      .onClick(() => {
        if (this.clickLikeStatus === undefined) {
          this.clickLike();
        }
      })
    }
    .justifyContent(FlexAlign.Center)
    .height('100%')
    .width('100%')
  }

 <em> // Image组件的点击事件，可以切换播放动画和显示图片</em>
  clickLike() {
    if (this.isLike) {
      this.clickLikeStatus = false;
      <em>// 播放取消点赞动画</em>
    } else {
      this.clickLikeStatus = true;
     <em> // 播放点赞动画</em>
    }
  }
}

<em>// 播放（取消）点赞动画，此处仅为模拟方案，实际按需替换</em>
@Component
struct LikePlayer {
  @Prop isLikeAnimation: boolean; <em>// 是否是点赞动画，有点赞动画和取消点赞动画</em>
  @State isLike: boolean = !this.isLikeAnimation; <em>// 当前点赞状态</em>
  @State scaleValue: number = 1; <em>// 缩放比例</em>
  @State showAnimation: boolean = true; <em>// 是否显示动画</em>
  <em>// 未点赞时的颜色(灰色)</em>
  unlikeColor: string = '#757575';
 <em> // 点赞后的颜色(蓝色)</em>
  likeColor: string = '#4eabe6';
  <em>// 点赞图片，请使用尺寸较小的本地图片</em>
  likeIcon: ResourceStr = $r('app.media.ic_public_thumbs_up');

  build() {
    Image(this.likeIcon)
      .syncLoad(true) <em>// 同步加载，避免闪烁</em>
      .objectFit(ImageFit.Contain) <em>// 缩放填充</em>
      .aspectRatio(1) <em>// 固定宽高比为1</em>
      .width(70)
      .fillColor(this.isLike ? this.likeColor : this.unlikeColor)
      .scale({ x: this.scaleValue, y: this.scaleValue }) <em>// 添加缩放效果</em>
      .onComplete(() => {
        <em>// 切换点赞状态</em>
        this.isLike = !this.isLike;
       <em> // 点赞动画-第一阶段：放大</em>
        this.getUIContext().animateTo({
          duration: 300, <em>// 动画持续300毫秒</em>
          curve: Curve.Friction, <em>// 使用摩擦曲线，效果更自然</em>
          delay: 0 <em>// 无延迟立即执行</em>
        }, () => {
          this.scaleValue = 1.5; <em>// 图标放大到1.5倍</em>
        });
       <em> // 点赞动画-第二阶段：恢复原大小</em>
        this.getUIContext().animateTo({
          duration: 100, <em>// 动画持续100毫秒</em>
          curve: Curve.Friction, <em>// 使用摩擦曲线</em>
          delay: 300 <em>// 延迟300毫秒执行，等第一阶段完成</em>
        }, () => {
          this.scaleValue = 1; <em>// 图标恢复到原始大小</em>

        });
       <em> // 模拟动画播放完毕后显示空白</em>
        setTimeout(() => {
          this.showAnimation = false;
        }, 400);
      })
      .visibility(this.showAnimation ? Visibility.Visible : Visibility.Hidden)
  }
}
```

 
 

#### 分析结论

点击点赞按钮时，未延时修改if/else条件渲染中使用的变量的值，在动画播放完毕后，未渲染Image组件，导致点赞图片消失。
 
 

#### 修改建议

在Image组件的点击事件中，延时修改if/else条件渲染中所使用的变量的值，保证点赞图片在动画播放完毕后正常渲染。
 
```text
@Entry
@Component
struct ClickLikeBtn {
  @State isLike: boolean = false; <em>// 点赞状态</em>
  @State clickLikeStatus: boolean | undefined = undefined; <em>// 根据布尔值判断使用取消点赞/点赞动画</em>
 <em> // 点赞图片，请使用尺寸较小的本地图片</em>
  likeIcon: ResourceStr = $r('app.media.ic_public_thumbs_up');
  <em>// 未点赞时的颜色(灰色)</em>
  unlikeColor: string = '#666666';
  <em>// 点赞后的颜色(蓝色)</em>
  likeColor: string = '#0a59f7';

  build() {
    Column() {
      Row() {
        Stack() {
          if (this.clickLikeStatus == undefined) {
            Image(this.likeIcon)
              .objectFit(ImageFit.Contain) <em>// 缩放填充</em>
              .aspectRatio(1) <em>// 固定宽高比为1</em>
              .width(70)
              .syncLoad(true) <em>// 同步加载，避免闪烁</em>
              .fillColor(this.isLike ? this.likeColor : this.unlikeColor)
              .onAppear(() => {
                console.info('[LikeIcon]', 'appear');
              })
              .onDisAppear(() => {
                console.info('[LikeIcon]', 'disappear');
              });
          } else {
           <em> // 自定义组件，用于播放动画</em>
            AnimationPlayer({ isLikeAnimation: this.clickLikeStatus });
          }
        };
      }
      .onClick(() => {
        if (this.clickLikeStatus === undefined) {
          this.clickLike();
        }
      });
    }
    .justifyContent(FlexAlign.Center)
    .height('100%')
    .width('100%');
  }

  <em>// Image组件的点击事件，可以切换播放动画和显示图片</em>
  clickLike() {
    if (this.isLike) {
      this.clickLikeStatus = false;
      <em>// 播放取消点赞动画</em>
    } else {
      this.clickLikeStatus = true;
     <em> // 播放点赞动画</em>
    }
   <em> // 使用计时器，延时修改（在动画播放完毕后修改）变量的值为undefined，渲染图片</em>
<em>    // 第二参数是点赞/取消点赞动画的时长</em>
    setTimeout(() => {
      this.isLike = !this.isLike;
      this.clickLikeStatus = undefined;
    }, 400);
  }
}

<em>// 播放（取消）点赞动画，此处仅为模拟方案，实际按需替换</em>
@Component
struct AnimationPlayer {
  @Prop isLikeAnimation: boolean; <em>// 是否是点赞动画，有点赞动画和取消点赞动画</em>
  @State isLike: boolean = !this.isLikeAnimation; <em>// 当前点赞状态</em>
  @State scaleValue: number = 1; <em>// 缩放比例</em>
 <em> // 未点赞时的颜色(灰色)</em>
  unlikeColor: string = '#666666';
<em>  // 点赞后的颜色(蓝色)</em>
  likeColor: string = '#0a59f7';
  <em>// 点赞图片，请使用尺寸较小的本地图片</em>
  likeIcon: ResourceStr = $r('app.media.ic_public_thumbs_up');

  build() {
    Image(this.likeIcon)
      .syncLoad(true) <em>// 同步加载，避免闪烁</em>
      .objectFit(ImageFit.Contain)<em> // 缩放填充</em>
      .aspectRatio(1) <em>// 固定宽高比为1</em>
      .width(70)
      .fillColor(this.isLike ? this.likeColor : this.unlikeColor)
      .scale({ x: this.scaleValue, y: this.scaleValue }) <em>// 添加缩放效果</em>
      .onComplete(() => {
       <em> // 切换点赞状态</em>
        this.isLike = !this.isLike;
       <em> // 点赞动画-第一阶段：放大</em>
        this.getUIContext().animateTo({
          duration: 300, <em>// 动画持续300毫秒</em>
          curve: Curve.Friction, <em>// 使用摩擦曲线，效果更自然</em>
          delay: 0<em> // 无延迟立即执行</em>
        }, () => {
          this.scaleValue = 1.5; <em>// 图标放大到1.5倍</em>
        });
       <em> // 点赞动画-第二阶段：恢复原大小</em>
        this.getUIContext().animateTo({
          duration: 100, <em>// 动画持续100毫秒</em>
          curve: Curve.Friction, <em>// 使用摩擦曲线</em>
          delay: 300 <em>// 延迟300毫秒执行，等第一阶段完成</em>
        }, () => {
          this.scaleValue = 1; <em>// 图标恢复到原始大小</em>
        });
      });
  }
}
```
