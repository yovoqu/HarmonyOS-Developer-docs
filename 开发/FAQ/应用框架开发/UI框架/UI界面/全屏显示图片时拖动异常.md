# 全屏显示图片时拖动异常

更新时间：2026-06-26 09:07:13

来源：https://developer.huawei.com/consumer/cn/doc/harmonyos-faqs/faqs-arkui-1563

#### 问题现象

场景一：全屏显示图片时，未放大图片也可拖动图片。
 

![](https://contentcenter-vali-drcn.dbankcdn.cn/pvt_2/DeveloperAlliance_scene_100_1/6d/v3/d_midsfdRF-7d2u4QP29YQ/zh-cn_image_0000002628609858.png?HW-CC-KV=V1&HW-CC-Date=20260701T041153Z&HW-CC-Expire=86400&HW-CC-Sign=1870821CC1C81F8AF99B3C5160CBCE48D645169275F1E9F078B4D1B3FE0C0AB2)

 
场景二：图片全屏显示后拖动图片到其他位置，停止拖动后图片无法回到原来位置，页面布局异常。
 

![](https://contentcenter-vali-drcn.dbankcdn.cn/pvt_2/DeveloperAlliance_scene_100_1/96/v3/HNTw9UYkTiSXGxXidyLKBQ/zh-cn_image_0000002628769752.png?HW-CC-KV=V1&HW-CC-Date=20260701T041153Z&HW-CC-Expire=86400&HW-CC-Sign=8C314EF8D4E3352B9D10156232BF3DEC3BD5FA4F185E4367CA528F446F0F28EB)

 
 

#### 背景知识

- [Image](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/ts-basic-components-image)为图片组件，常用于在应用中显示图片。
- [PanGesture](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/ts-basic-gestures-pangesture)为滑动手势事件，当滑动的最小距离达到设定的最小值时触发滑动手势事件。
- [PinchGesture](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/ts-basic-gestures-pinchgesture)用于触发捏合手势，触发捏合手势的最少手指为2指，最大为5指，最小识别距离为5vp。
- [组合手势](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/ts-combined-gestures)即多种手势组合为复合手势，支持连续识别、并行识别和互斥识别。通过[GestureGroup接口](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/ts-combined-gestures#接口)实现。
- [translate](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/ts-universal-attributes-transformation#translate)可设置组件平移。

 
 

#### 问题定位

场景一：
 1. 使用DevEco Testing查看问题组件，该组件为Image组件。
![](https://contentcenter-vali-drcn.dbankcdn.cn/pvt_2/DeveloperAlliance_scene_100_1/10/v3/XxumiPX2RKCHZnkNxjEU7w/zh-cn_image_0000002658969073.png?HW-CC-KV=V1&HW-CC-Date=20260701T041153Z&HW-CC-Expire=86400&HW-CC-Sign=EB6656AEF84ED7E6B518B3AB9CBF33354965381A8B0487A32C5553E58E6F550A)

2. 查看Image组件的设置，该组件绑定了PinchGesture和PanGesture，实现图片放大缩小和图片移动功能，且没有对图片未放大时的交互做限制，未放大时也能移动。正常情况下应用能够放大图片并移动图片查看细节。
```text
Stack() {
  Image($r('app.media.product003')) <em>// $r('app.media.product003')需要替换为开发者需要的图片资源文件</em>
    .width('80%')
    .objectFit(ImageFit.Contain)
    .backgroundColor(Color.White)
    .translate({ x: this.offsetX, y: this.offsetY, z: 0 }) <em>// 移动图片</em>
    .scale({ x: this.scaleValue, y: this.scaleValue, z: 1 }); <em>// 缩放图片</em>
}
.height('100%')
.width('100%')
.gesture(
  GestureGroup(GestureMode.Exclusive,
  <em>  // 双指缩放</em>
    PinchGesture({ fingers: 2 })
      .onActionUpdate((event: GestureEvent) => {
        if (event) {
          this.scaleValue = this.pinchValue * event.scale;
        }
      })
      .onActionEnd(() => {
        this.pinchValue = this.scaleValue;
      })
    ,
    <em>// 拖动图片，且未放大时也能移动</em>
    PanGesture(this.panOption)
      .onActionUpdate((event: GestureEvent) => {
        if (event) {
          this.offsetX = this.lastX + event.offsetX;
          this.offsetY = this.lastY + event.offsetY;
        }
      })
      .onActionEnd(() => {
        this.lastX = this.offsetX;
        this.lastY = this.offsetY;
      })
  )
)
```

 
场景二：
 1. 使用DevEco Testing查看问题组件，该问题组件为Image组件。
![](https://contentcenter-vali-drcn.dbankcdn.cn/pvt_2/DeveloperAlliance_scene_100_1/33/v3/yMBSADOcSASCK5RDTX3VQg/zh-cn_image_0000002658849121.png?HW-CC-KV=V1&HW-CC-Date=20260701T041153Z&HW-CC-Expire=86400&HW-CC-Sign=9E23C901BECD56A2EA3884269644374295F223922F727D0A3601F8F51CD7884E)

2. 查看该Image组件的设置，Image组件通过PanGesture响应滑动手势事件，并通过translate实现平移功能。然而组件在onActionEnd中设置了滑动结束后更新图片位置，导致图片后无法回到原来位置。
```text
@Component
export struct Dialog {
  pageInfos: NavPathStack = new NavPathStack();
  @State offsetX: number = 0;
  @State offsetY: number = 0;
  @State positionX: number = 0;
  @State positionY: number = 0;
  private panOption: PanGestureOptions = new PanGestureOptions({ direction: PanDirection.Vertical });

  build() {
    NavDestination() {
      Stack() {
        Column()
        Image($r('app.media.beauty')) <em>// $r('app.media.beauty')需要替换为开发者</em>
          .translate({ x: this.offsetX, y: this.offsetY }) <em>// 平移图片</em>
      }
      .height('100%')
      .width('100%')
      .gesture(
        PanGesture(this.panOption)
          .onActionUpdate((event: GestureEvent) => {
            if (event) {
              this.offsetX = this.positionX + event.offsetX;
              this.offsetY = this.positionY + event.offsetY;
            }
          })
          .onActionEnd((event: GestureEvent) => {
          <em>  // 滑动结束后更新图片位置</em>
            this.positionX = this.offsetX;
            this.positionY = this.offsetY;
          })
      )
    }
  }
}
```

 
 

#### 分析结论

场景一：应用设置了图片能够放大缩小和移动，但没有对图片未放大时的交互做限制，导致全屏显示图片时，未放大的图片也能够响应拖拽手势。
 
场景二：Image组件在onActionEnd中设置了拖动结束后更新图片位置，导致停止拖动图片后无法回到原来位置。
 
 

#### 修改建议

- 场景一：设置图片放大后才能拖动，避免未放大时图片未固定。

  关键代码如下：

  
```text
isDragable() {
  if (this.scaleValue > 1) {
   <em> // 放大后才可拖动图片</em>
    this.myDragable = true;
  } else {
   <em> // 原来尺寸或缩小时无法拖动图片</em>
    this.myDragable = false;
  }
}
```
 
```text
.gesture(
  GestureGroup(GestureMode.Exclusive,
    <em>// 双指缩放</em>
    PinchGesture({ fingers: 2 })
      .onActionUpdate((event: GestureEvent) => {
        if (event) {
          this.scaleValue = this.pinchValue * event.scale; <em>// 获取缩放比例</em>
        }
      })
      .onActionEnd(() => {
        this.pinchValue = this.scaleValue;
      })
    ,
   <em> // 拖动图片</em>
    PanGesture(this.panOption)
      .onActionUpdate((event: GestureEvent) => {
        <em>// 缩放比例大于1时才可拖动</em>
        if (event && this.myDragable) {
          this.offsetX = this.lastX + event.offsetX;
          this.offsetY = this.lastY + event.offsetY;
        }
      })
```


 
完整示例如下：
 
src/main/ets/pages/SituationOne.ets：
 
```text
import { window } from '@kit.ArkUI';
import { common } from '@kit.AbilityKit';
import { BusinessError } from '@kit.BasicServicesKit';

@Entry
@Component
struct SituationOne {
  pageInfos: NavPathStack = new NavPathStack();

  aboutToAppear() {
    this.pageInfos.pushPath({ name: 'SituationOne_PageMain' }, false);
  }

  build() {
    Stack() {
      Navigation(this.pageInfos)
        .hideNavBar(true)
        .width('100%')
        .height('100%');
    }
    .width('100%')
    .height('100%')
    .expandSafeArea([SafeAreaType.SYSTEM], [SafeAreaEdge.TOP, SafeAreaEdge.BOTTOM]);
  }
}

@Builder
export function SituationOne_PageMainBuilder() {
  SituationOne_PageMain();
}

@Builder
export function SituationOne_DialogBuilder() {
  SituationOne_Dialog();
}

<em>// 通过弹窗全屏显示图片</em>
@Component
export struct SituationOne_Dialog {
  pageInfos: NavPathStack = new NavPathStack();
  @State offsetX: number = 0;
  @State offsetY: number = 0;
  @State @Watch('isDragable') scaleValue: number = 1;
  myDragable: boolean = false;
  pinchValue: number = 1;
  private lastX: number = 0;
  private lastY: number = 0;
  private panOption: PanGestureOptions = new PanGestureOptions({ direction: PanDirection.All });

  isDragable() {
    if (this.scaleValue > 1) {
     <em> // 放大后才可拖动图片</em>
      this.myDragable = true;
    } else {
    <em>  // 原来尺寸或缩小时无法拖动图片</em>
      this.myDragable = false;
    }
  }

  aboutToAppear(): void {
    let context: common.UIAbilityContext = this.getUIContext().getHostContext() as common.UIAbilityContext;
    window.getLastWindow(context).then((lastWindow) => {
      lastWindow.setWindowLayoutFullScreen(true);
      lastWindow.setWindowSystemBarEnable([]);
    }).catch((err: BusinessError) => {
      console.error(err.message, err.code);
    });
  }

  aboutToDisappear(): void {
    let context: common.UIAbilityContext = this.getUIContext().getHostContext() as common.UIAbilityContext;
    window.getLastWindow(context).then((lastWindow) => {
      lastWindow.setWindowLayoutFullScreen(false);
      lastWindow.setWindowSystemBarEnable(['status', 'navigation']);
    }).catch((err: BusinessError) => {
      console.error(err.message, err.code);
    });
  }

  build() {
    NavDestination() {
      Stack() {
        Image($r('app.media.beauty')) <em>// $r('app.media.beauty')需要替换为开发者需要的图片资源</em>
          .width('80%')
          .objectFit(ImageFit.Contain)
          .backgroundColor(Color.White)
          .translate({ x: this.offsetX, y: this.offsetY, z: 0 }) <em>// 移动图片</em>
          .scale({ x: this.scaleValue, y: this.scaleValue, z: 1 }); <em>// 缩放图片</em>
      }
      .height('100%')
      .width('100%')
      .gesture(
        GestureGroup(GestureMode.Exclusive,
        <em>  // 双指缩放</em>
          PinchGesture({ fingers: 2 })
            .onActionUpdate((event: GestureEvent) => {
              if (event) {
                this.scaleValue = this.pinchValue * event.scale; <em>// 获取缩放比例</em>
              }
            })
            .onActionEnd(() => {
              this.pinchValue = this.scaleValue;
            })
          ,
         <em> // 拖动图片</em>
          PanGesture(this.panOption)
            .onActionUpdate((event: GestureEvent) => {
             <em> // 缩放比例大于1时才可拖动</em>
              if (event && this.myDragable) {
                this.offsetX = this.lastX + event.offsetX;
                this.offsetY = this.lastY + event.offsetY;
              }
            })
            .onActionEnd(() => {
              this.lastX = this.offsetX;
              this.lastY = this.offsetY;
            })
        )
      )
      .onClick(() => {
        this.pageInfos.pop();
      })
      .expandSafeArea([SafeAreaType.SYSTEM], [SafeAreaEdge.TOP, SafeAreaEdge.BOTTOM]);
    }
    .hideTitleBar(true)
    .systemTransition(NavigationSystemTransitionType.NONE)
    .onReady((context: NavDestinationContext) => {
      this.pageInfos = context.pathStack;
    })
    .height('100%')
    .width('100%')
    .backgroundColor(Color.Black)
    .mode(NavDestinationMode.DIALOG)
    .expandSafeArea([SafeAreaType.SYSTEM], [SafeAreaEdge.TOP, SafeAreaEdge.BOTTOM]);
  }
}

@Component
export struct SituationOne_PageMain {
  pageInfos: NavPathStack = new NavPathStack();

  build() {
    NavDestination() {
      Stack() {
        Image($r('app.media.beauty')) <em>// $r('app.media.beauty')需要替换为开发者需要的图片资源</em>
          .width(200)
          .height(200)
          .objectFit(ImageFit.Contain)
          .onClick(() => {
            this.pageInfos.pushPath({ name: 'SituationOne_Dialog' });
          });
      }
      .width('100%')
      .height('100%');
    }
    .hideTitleBar(true)
    .onReady((context: NavDestinationContext) => {
      this.pageInfos = context.pathStack;
    })
    .height('100%')
    .width('100%')
    .mode(NavDestinationMode.STANDARD);
  }
}
```
 
运行效果如下：
 

![](https://contentcenter-vali-drcn.dbankcdn.cn/pvt_2/DeveloperAlliance_scene_100_1/5a/v3/qAxIX8qPTEKAzghe0biCJg/zh-cn_image_0000002628609860.png?HW-CC-KV=V1&HW-CC-Date=20260701T041153Z&HW-CC-Expire=86400&HW-CC-Sign=167E42830A967082F0F5F047757DCA116D849FC89771186FCB4CD69A5AF05F75)

 
 
- 场景二：在onActionEnd中设置滑动结束后回到原来位置。

  关键代码如下：

  
```text
Stack() {
  Column()
    .width('100%')
    .height('100%')
    .backgroundColor(Color.Black)
    .opacity(this.myOpacity);
  Image($r('app.media.beauty')) <em>// $r('app.media.beauty')需要替换为开发者需要的图片资源</em>
    .width('80%')
    .backgroundColor(Color.White)
    .objectFit(ImageFit.Contain)
    .translate({ x: this.offsetX, y: this.offsetY }); <em>// 图片平移</em>
}
.height('100%')
.width('100%')
.gesture(
  PanGesture(this.panOption)
    .onActionUpdate((event: GestureEvent) => {
      if (event) {
        <em>// 平移图片</em>
        this.offsetX = event.offsetX;
        this.offsetY = event.offsetY;
        this.myOpacity = 0;
        window.getLastWindow(this.context).then((lastWindow) => {
          lastWindow.setWindowLayoutFullScreen(false);
          lastWindow.setWindowSystemBarEnable(['status', 'navigation']);
        }).catch((err: BusinessError) => {
          console.error(err.message, err.code);
        });
      }
    })
    .onActionEnd(() => {
     <em> // 滑动结束后回到原来位置</em>
      this.myOpacity = 1;
      this.offsetX = 0;
      this.offsetY = 0;
      window.getLastWindow(this.context).then((lastWindow) => {
        lastWindow.setWindowLayoutFullScreen(true);
        lastWindow.setWindowSystemBarEnable([]);
      }).catch((err: BusinessError) => {
        console.error(err.message, err.code);
      });
    })
```


 
完整示例如下：
 
src/main/ets/pages/SituationTwo.ets：
 
```text
import { window } from '@kit.ArkUI';
import { common } from '@kit.AbilityKit';
import { BusinessError } from '@kit.BasicServicesKit';

@Entry
@Component
struct SituationTwo {
  pageInfos: NavPathStack = new NavPathStack();

  aboutToAppear() {
    this.pageInfos.pushPath({ name: 'SituationTwo_PageMain' }, false);
  }

  build() {
    Stack() {
      Navigation(this.pageInfos)
        .hideNavBar(true)
        .width('100%')
        .height('100%');
    }
    .width('100%')
    .height('100%')
    .alignContent(Alignment.Top)
    .expandSafeArea([SafeAreaType.SYSTEM], [SafeAreaEdge.TOP, SafeAreaEdge.BOTTOM]);
  }
}

@Builder
export function SituationTwo_PageMainBuilder() {
  SituationTwo_PageMain();
}

@Builder
export function SituationTwo_DialogBuilder() {
  SituationTwo_Dialog();
}

@Component
export struct SituationTwo_Dialog {
  pageInfos: NavPathStack = new NavPathStack();
  @State offsetX: number = 0;
  @State offsetY: number = 0;
  @State myOpacity: number = 1;
  private panOption: PanGestureOptions = new PanGestureOptions({ direction: PanDirection.Vertical });
  private context: common.UIAbilityContext = this.getUIContext().getHostContext() as common.UIAbilityContext;

  aboutToAppear(): void {
    window.getLastWindow(this.context).then((lastWindow) => {
      lastWindow.setWindowSystemBarEnable([]);
    });
  }

  aboutToDisappear(): void {
    window.getLastWindow(this.context).then((lastWindow) => {
      lastWindow.setWindowSystemBarEnable(['status', 'navigation']);
    }).catch((err: BusinessError) => {
      console.error(err.message, err.code);
    });
  }

  build() {
    NavDestination() {
      Stack() {
        Column()
          .width('100%')
          .height('100%')
          .backgroundColor(Color.Black)
          .opacity(this.myOpacity);
        Image($r('app.media.beauty')) <em>// $r('app.media.beauty')需要替换为开发者需要的图片资源</em>
          .width('80%')
          .backgroundColor(Color.White)
          .objectFit(ImageFit.Contain)
          .translate({ x: this.offsetX, y: this.offsetY }); <em>// 图片平移</em>
      }
      .height('100%')
      .width('100%')
      .gesture(
        PanGesture(this.panOption)
          .onActionUpdate((event: GestureEvent) => {
            if (event) {
             <em> // 平移图片</em>
              this.offsetX = event.offsetX;
              this.offsetY = event.offsetY;
              this.myOpacity = 0;
              window.getLastWindow(this.context).then((lastWindow) => {
                lastWindow.setWindowLayoutFullScreen(false);
                lastWindow.setWindowSystemBarEnable(['status', 'navigation']);
              }).catch((err: BusinessError) => {
                console.error(err.message, err.code);
              });
            }
          })
          .onActionEnd(() => {
            <em>// 滑动结束后回到原来位置</em>
            this.myOpacity = 1;
            this.offsetX = 0;
            this.offsetY = 0;
            window.getLastWindow(this.context).then((lastWindow) => {
              lastWindow.setWindowLayoutFullScreen(true);
              lastWindow.setWindowSystemBarEnable([]);
            }).catch((err: BusinessError) => {
              console.error(err.message, err.code);
            });
          })
      )
      .onClick(() => {
        this.pageInfos.pop();
      })
      .backgroundColor('#00000000')
      .expandSafeArea([SafeAreaType.SYSTEM], [SafeAreaEdge.TOP, SafeAreaEdge.BOTTOM]);
    }
    .hideTitleBar(true)
    .systemTransition(NavigationSystemTransitionType.NONE)
    .onReady((context: NavDestinationContext) => {
      this.pageInfos = context.pathStack;
    })
    .height('100%')
    .width('100%')
    .backgroundColor('#00000000')
    .mode(NavDestinationMode.DIALOG)
    .expandSafeArea([SafeAreaType.SYSTEM], [SafeAreaEdge.TOP, SafeAreaEdge.BOTTOM]);
  }
}

@Component
export struct SituationTwo_PageMain {
  pageInfos: NavPathStack = new NavPathStack();

  build() {
    NavDestination() {
      Stack() {
        Image($r('app.media.beauty')) <em>// $r('app.media.beauty')需要替换为开发者需要的图片资源</em>
          .width(200)
          .height(200)
          .objectFit(ImageFit.Contain)
          .onClick(() => {
            this.pageInfos.pushPath({ name: 'SituationTwo_Dialog' }); <em>// 跳转到弹窗页面</em>
          });
      }
      .width('100%')
      .height('100%');
    }
    .hideTitleBar(true)
    .onReady((context: NavDestinationContext) => {
      this.pageInfos = context.pathStack;
    })
    .height('100%')
    .width('100%')
    .mode(NavDestinationMode.STANDARD);
  }
}
```
 
运行效果如下：
 

![](https://contentcenter-vali-drcn.dbankcdn.cn/pvt_2/DeveloperAlliance_scene_100_1/55/v3/xurzBUTjTmCIHSzjIVAR2A/zh-cn_image_0000002628769756.png?HW-CC-KV=V1&HW-CC-Date=20260701T041153Z&HW-CC-Expire=86400&HW-CC-Sign=E5CC6FEACEB4D2B5E0B05CCCE49453AC6B60B11F5ED018FCCAA7B62BD150A769)

 
- src/main/module.json5文件中需添加"routerMap": "$profile:router_map"，且需添加如下所示的src/main/resources/base/profile/router_map.json文件。SituationOne_PageMain和SituationOne_Dialog为场景一的内容，SituationTwo_PageMain和SituationTwo_Dialog为场景二的内容。
```ArkTS
{
  "routerMap": [
    {
      "name": "SituationOne_PageMain",
      "pageSourceFile": "src/main/ets/pages/SituationOne.ets",
      "buildFunction": "SituationOne_PageMainBuilder"
    },
    {
      "name": "SituationOne_Dialog",
      "pageSourceFile": "src/main/ets/pages/SituationOne.ets",
      "buildFunction": "SituationOne_DialogBuilder"
    },
    {
      "name": "SituationTwo_PageMain",
      "pageSourceFile": "src/main/ets/pages/SituationTwo.ets",
      "buildFunction": "SituationTwo_PageMainBuilder"
    },
    {
      "name": "SituationTwo_Dialog",
      "pageSourceFile": "src/main/ets/pages/SituationTwo.ets",
      "buildFunction": "SituationTwo_DialogBuilder"
    }
  ]
}
```
