# 在设置中修改字体大小和显示缩放大小后，应用页面布局异常

更新时间：2026-06-26 09:07:13

来源：https://developer.huawei.com/consumer/cn/doc/harmonyos-faqs/faqs-ability-140

#### 问题现象

在设备设置中，修改字体大小以及显示大小缩放后，页面中布局出现异常（如出现组件间遮挡、截断、布局错乱等问题）。
 
 

#### 背景知识

- [订阅环境变量](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/subscribe-system-environment-variable-changes#订阅环境变量)：系统配置的变化通常由“设置”中的选项或“控制中心”中的图标触发。订阅环境变量变化，可以使应用程序更加智能地响应系统环境变化，从而提供更好的用户体验。
- [configuration标签](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/app-configuration-file#configuration标签)：可以在app.json5配置文件中通过configuration标签，配置应用字体大小是否跟随系统变更以及应用字体最大放大倍数。
- [Configuration](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/js-apis-app-ability-configuration#configuration)：定义了应用运行时的环境变量，包含语言、深浅色、屏幕方向、字体等。其中fontSizeScale表示字体大小缩放比例，受设置中的字体大小影响；screenDensity表示屏幕像素密度，受设置中的显示大小缩放影响。
- [支持适老化](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/arkui-support-for-aging-adaptation)：系统字体被放大后，应用应确保整体布局不出现错乱，组件不出现重叠。可以根据业务需要限制跟随的字体最大档位、改变布局来更好的适配更大字体等。
- [maxFontScale](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/ts-basic-components-text#maxfontscale12)：设置文本最大的字体缩放倍数。
- [densityUpdate](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/arkts-apis-uicontext-uiobserver#ondensityupdate12)监听事件：监听屏幕像素密度变化。当设置中的显示大小缩放变化后，[屏幕实例](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/js-apis-display#display)中的densityDPI会随之变化。
- [onSizeChange](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/ts-universal-component-size-change-event#onsizechange)：组件尺寸变化事件。组件区域变化时触发该回调。仅会响应由布局变化所导致的组件尺寸发生变化时的回调。

 
 

#### 问题定位

- 排查是否订阅环境变量。排查使用[onConfigurationUpdate](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/js-apis-app-ability-ability#abilityonconfigurationupdate)回调方法中，根据fontSizeScale、screenDensity属性全局调整应用页面布局（推荐应用采用响应式布局并在该方法中更新断点）。
- 排查是否限制字体大小，以及数值是否合理。排查app.json5文件中configuration字段指向的资源文件中，是否存在fontSizeMaxScale字段，并检查该值是否合适。排查组件是否设置maxFontScale属性以及其数值是否合适。
- 排查是否订阅环境变量、densityUpdate事件，并在调整页面布局。排查是否监听onSizeChange事件，并在其中调整页面布局。

 
 

#### 分析结论

- 应用未订阅环境变量，并根据环境变量中字体缩放倍数和屏幕像素密度对页面布局进行调整适应，导致页面布局出现异常情况。
- 应用未限制显示的字体缩放倍数或者配置的字体最大或最小缩放倍数不合适，在改变了系统字体大小和显示缩放大小后，出现字体超出容器显示或者超出背景区域的情况，导致布局异常。
- 应用未监听屏幕像素密度变化并调整页面布局，也未对某个组件添加onSizeChange事件并调整组件大小布局。导致调整显示大小缩放后，页面组件布局不符合预期，布局异常。

 
 

#### 修改建议

- 如果需要全局对该情况进行适配，推荐参考[应用适配适老化大字体](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/arkui-support-for-aging-adaptation#应用适配适老化大字体)、[响应式布局](https://developer.huawei.com/consumer/cn/doc/best-practices/bpta-multi-device-responsive-layout)以及[限定词目录](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/resource-categories-and-access#限定词目录)，使用适配不同屏幕DPI的资源文件管理页面布局，并在onConfigurationUpdate中调整断点和布局。
```json
onConfigurationUpdate(newConfig: Configuration): void {
  console.info(`[ConfigurationChange], config: ${JSON.stringify(newConfig)}`);
  AppStorage.setOrCreate('FontSizeScale', newConfig.fontSizeScale); <em>// 字体缩放比例</em>
  AppStorage.setOrCreate('ScreenDensity', newConfig.screenDensity); <em>// 屏幕像素密度</em>

  <em>// 如果应用采用响应式布局，可以根据字体缩放比例和屏幕像素密度更新断点</em>
<em>  // 或者根据fontSizeScale、screenDensity的值直接调整页面布局（限制组件宽高、换行显示、超出省略等方案）</em>
}
```

- 限制字体缩放倍数，保证字体相关布局正常。
如果仅仅是对特定某个组件中的字体大小进行限制，推荐采用maxFontScale属性，设置字体最大缩放倍数。保证页面布局正常。
```text
@Component
export struct MaxScale {
  build() {
    Column() {
    <em>  // 只会限制字体大小，实际显示效果还是会受到显示缩放大小的影响</em>
      Text('限制字体最大缩放倍数')
        .maxFontScale(1.15)
    }
  }
}
```

- 在app.json5文件中，修改configuration标签指向的资源文件，配置fontSizeMaxScale设置应用字体最大缩放倍数。示例配置如下，限制字体最大缩放倍数为1.45。
```json
{
  "configuration": {
    "fontSizeScale": "followSystem",
    "fontSizeMaxScale": "1.45"
  }
}
```

- 如果有需要的话，可以配置应用字体不跟随系统设置变化，在应用内自行统一管理字体。可以参考[应用内字体大小调节功能](https://developer.huawei.com/consumer/cn/codelabsPortal/carddetails/tutorials_NEXT-SetAppFontSize)。

 - 订阅相关事件，并在其中限制组件大小、调整页面布局，如订阅环境变量关注屏幕显示密度、添加densityUpdate或onSizeChange监听事件。
```text
import { display, uiObserver, window } from '@kit.ArkUI';
import { BusinessError } from '@ohos.base';

@Component
export struct DensityUpdateTest {
  parentPadding: number = 12;<em> // 左右边距</em>
<em>  // 限制组件最大宽度</em>
  @State maxWidth: number =
    (this.getUIContext().px2vp(display.getDefaultDisplaySync().width) / 2 - this.parentPadding) / 2;

  aboutToAppear(): void {
    this.getUIContext()
      .getUIObserver()
      .on('densityUpdate', (info: uiObserver.DensityInfo) => {
      <em>  // 如果采用了响应式布局，可以直接更新断点，根据断点调整布局</em>
        console.info('[densityChange]', `current width breakpoint:`, this.getUIContext().getWindowWidthBreakpoint());

        <em>// 也可以根据density数据计算组件大小，调整布局</em>
<em>        // vp和pv转换公式：`vp = px / (DPI/160)` 和`info.density`相关</em>
<em>        // 建议直接通过UIContext的px2vp或vp2px方法转换</em>
        console.info('[densityChange]', `current density: ${info.density}`);

        <em>// 此处仅为示例，可以根据实际需要选择合适布局调整方案</em>
<em>        // 获取窗口宽度，并调整组件的最大宽度</em>
        window.getLastWindow(this.getUIContext().getHostContext(), (err: BusinessError, windowClass: window.Window) => {
          let windowWidth = this.getUIContext().px2vp(windowClass.getGlobalRect().width);
      <em>    // 设置组件最大宽度，为(窗口左半-边距)/2，左侧组件占一半，右侧组件的一半宽度也不能超过该值</em>
          this.maxWidth = (windowWidth / 2 - this.parentPadding) / 2;
        });
      });
  }

  build() {
    Column() {
      Column() {
        Stack() {
          Row() {
            Text('布局测试-偏左')
              .backgroundColor('#ff92f1b0')
              .constraintSize({ maxWidth: this.maxWidth })<em> // 限制最大宽度</em>
          }
          .width('100%')
          .onSizeChange((oldSize, newSize) => {
        <em>    // 也可以在onSizeChange事件中调整布局</em>
            console.info('[CompSizeChange]', oldSize.width, newSize.width);
          })

          Row() {
            Text('布局测试-居中')
              .backgroundColor('#ffffcfcf')
              .constraintSize({ maxWidth: this.maxWidth * 2 })<em> // 限制最大宽度</em>
          }
        }
        .padding({
          left: this.parentPadding,
          right: this.parentPadding
        })
      }
      .width('100%')
    }
    .width('100%')
    .padding({
      top: 50,
      bottom: 30
    })
  }
}
```


 
完整示例参考如下：
 
- “ets/entryability/EntryAbility.ets”文件。
```json
import { AbilityConstant, Configuration, ConfigurationConstant, UIAbility, Want } from '@kit.AbilityKit';
import { hilog } from '@kit.PerformanceAnalysisKit';
import { window } from '@kit.ArkUI';

const DOMAIN = 0x0000;

export default class EntryAbility extends UIAbility {
  onCreate(want: Want, launchParam: AbilityConstant.LaunchParam): void {
    try {
      this.context.getApplicationContext().setColorMode(ConfigurationConstant.ColorMode.COLOR_MODE_NOT_SET);
    } catch (err) {
      hilog.error(DOMAIN, 'testTag', 'Failed to set colorMode. Cause: %{public}s', JSON.stringify(err));
    }
    hilog.info(DOMAIN, 'testTag', '%{public}s', 'Ability onCreate');
    hilog.info(DOMAIN, 'testTag', '%{public}s', JSON.stringify(launchParam));
  }

  onDestroy(): void {
    hilog.info(DOMAIN, 'testTag', '%{public}s', 'Ability onDestroy');
  }

  onWindowStageCreate(windowStage: window.WindowStage): void {
  <em>  // Main window is created, set main page for this ability</em>
    hilog.info(DOMAIN, 'testTag', '%{public}s', 'Ability onWindowStageCreate');

    windowStage.loadContent('pages/LayoutTest', (err) => {
      if (err.code) {
        hilog.error(DOMAIN, 'testTag', 'Failed to load the content. Cause: %{public}s', JSON.stringify(err));
        return;
      }
      hilog.info(DOMAIN, 'testTag', 'Succeeded in loading the content.');
    });
  }

  onWindowStageDestroy(): void {
   <em> // Main window is destroyed, release UI related resources</em>
    hilog.info(DOMAIN, 'testTag', '%{public}s', 'Ability onWindowStageDestroy');
  }

  onForeground(): void {
 <em>   // Ability has brought to foreground</em>
    hilog.info(DOMAIN, 'testTag', '%{public}s', 'Ability onForeground');
  }

  onBackground(): void {
   <em> // Ability has back to background</em>
    hilog.info(DOMAIN, 'testTag', '%{public}s', 'Ability onBackground');
  }

  onConfigurationUpdate(newConfig: Configuration): void {
    console.info(`[ConfigurationChange], config: ${JSON.stringify(newConfig)}`);
    AppStorage.setOrCreate('FontSizeScale', newConfig.fontSizeScale); <em>// 字体缩放比例</em>
    AppStorage.setOrCreate('ScreenDensity', newConfig.screenDensity); <em>// 屏幕像素密度</em>

    <em>// 如果应用采用响应式布局，可以根据字体缩放比例和屏幕像素密度更新断点</em>
<em>    // 或者根据fontSizeScale、screenDensity的值直接调整页面布局（限制组件宽高、换行显示、超出省略等方案）</em>
  }
}
```

- “ets/pages/LayoutTest.ets”文件。
```text
import { DensityUpdateTest } from '../component/DensityUpdateTest';
import { MaxScale } from '../component/MaxScaleTest';

@Entry
@Component
struct LayoutTest {
  build() {
    Column() {
      DensityUpdateTest()
      MaxScale()
    }
  }
}
```

- “ets/component/DensityUpdateTest.ets”文件。
```text
import { display, uiObserver, window } from '@kit.ArkUI';
import { BusinessError } from '@ohos.base';

@Component
export struct DensityUpdateTest {
  parentPadding: number = 12;<em> // 左右边距</em>
 <em> // 限制组件最大宽度</em>
  @State maxWidth: number =
    (this.getUIContext().px2vp(display.getDefaultDisplaySync().width) / 2 - this.parentPadding) / 2;

  aboutToAppear(): void {
    this.getUIContext()
      .getUIObserver()
      .on('densityUpdate', (info: uiObserver.DensityInfo) => {
     <em>   // 如果采用了响应式布局，可以直接更新断点，根据断点调整布局</em>
        console.info('[densityChange]', `current width breakpoint:`, this.getUIContext().getWindowWidthBreakpoint());

       <em> // 也可以根据density数据计算组件大小，调整布局</em>
<em>        // vp和pv转换公式：`vp = px / (DPI/160)` 和`info.density`相关</em>
<em>        // 建议直接通过UIContext的px2vp或vp2px方法转换</em>
        console.info('[densityChange]', `current density: ${info.density}`);

     <em>   // 此处仅为示例，可以根据实际需要选择合适布局调整方案</em>
<em>        // 获取窗口宽度，并调整组件的最大宽度</em>
        window.getLastWindow(this.getUIContext().getHostContext(), (err: BusinessError, windowClass: window.Window) => {
          let windowWidth = this.getUIContext().px2vp(windowClass.getGlobalRect().width);
     <em>     // 设置组件最大宽度，为(窗口左半-边距)/2，左侧组件占一半，右侧组件的一半宽度也不能超过该值</em>
          this.maxWidth = (windowWidth / 2 - this.parentPadding) / 2;
        });
      });
  }

  build() {
    Column() {
      Column() {
        Stack() {
          Row() {
            Text('布局测试-偏左')
              .backgroundColor('#ff92f1b0')
              .constraintSize({ maxWidth: this.maxWidth }) <em>// 限制最大宽度</em>
          }
          .width('100%')
          .onSizeChange((oldSize, newSize) => {
       <em>     // 也可以在onSizeChange事件中调整布局</em>
            console.info('[CompSizeChange]', oldSize.width, newSize.width);
          })

          Row() {
            Text('布局测试-居中')
              .backgroundColor('#ffffcfcf')
              .constraintSize({ maxWidth: this.maxWidth * 2 })<em> // 限制最大宽度</em>
          }
        }
        .padding({
          left: this.parentPadding,
          right: this.parentPadding
        })
      }
      .width('100%')
    }
    .width('100%')
    .padding({
      top: 50,
      bottom: 30
    })
  }
}
```

- “ets/component/MaxScaleTest.ets”文件。
```text
@Component
export struct MaxScale {
  build() {
    Column() {
   <em>   // 只会限制字体大小，实际显示效果还是会受到显示缩放大小的影响</em>
      Text('限制字体最大缩放倍数')
        .maxFontScale(1.15)
    }
  }
}
```
