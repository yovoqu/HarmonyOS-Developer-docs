# 如何在使用移动流量时进行相应提示

更新时间：2026-06-26 07:48:29

来源：https://developer.huawei.com/consumer/cn/doc/harmonyos-faqs/faqs-network-128

#### 问题现象

用户切换网络至移动流量时，应用应进行相应提示，然而应用未进行提示。
 
 

#### 背景知识

- [on('netCapabilitiesChange')](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/js-apis-net-connection#onnetcapabilitieschange)可订阅网络能力变化事件。
- 设备从Wi-Fi到移动数据会触发netLost事件（Wi-Fi丢失）之后触发netAvailable事件（移动数据可用）、netCapabilitiesChange事件和netConnectionPropertiesChange事件。
- [connection.getNetCapabilities](https://developer.huawei.com/consumer/cn/doc/harmonyos-references/js-apis-net-connection#connectiongetnetcapabilities)可获取netHandle对应网络的能力信息，使用callback方式作为异步方法。

 
 

#### 解决方案

使用getNetCapabilities获取进入页面时的网络类型，并使用on('netCapabilitiesChange')监听网络类型变化。关键内容如下：
 
```json
aboutToAppear(): void {
  <em>// 获取页面创建时的网络类型</em>
  connection.getDefaultNet()
    .then((netHandle: connection.NetHandle) => {
      if (netHandle.netId === 0) {
        <em>// 当前没有已连接的网络时，netHandle的netId为0，属于异常场景。</em>
        return;
      }
      connection.getNetCapabilities(netHandle, (error: BusinessError, data: connection.NetCapabilities) => {
        if (error) {
          return;
        }
        if (data.bearerTypes.length > 0) {
          let netType = data.bearerTypes[0]; <em>// 获取网络类型</em>
          <em>// 使用流量</em>
          if (netType === connection.NetBearType.BEARER_CELLULAR) {
            this.showDialog = true;
          } else {
            this.change = true;
          }
        }
      });
    })
    .catch((error: BusinessError) => {
      console.error(`Failed to get default net. Code:${error.code}, message:${error.message}`);
    });

 <em> // 监听网络类型切换</em>
  this.netCon.on('netCapabilitiesChange', (data: connection.NetCapabilityInfo) => {
    if (data.netCap.bearerTypes.length > 0) {
      let netType = data.netCap.bearerTypes[0];<em> // 获取当前网络类型</em>
      <em>// 当前网络为数据流量时进行提示</em>
      if (netType === connection.NetBearType.BEARER_CELLULAR && this.change) {
        this.showDialog = true;
      }
    }
  });

 <em> // 使用register接口注册网络状态变化事件。</em>
  this.netCon.register((error: BusinessError) => {
    console.error(JSON.stringify(error));
  });
}
```
 
完整示例如下：
 
```json
import { connection } from '@kit.NetworkKit';
import { BusinessError } from '@kit.BasicServicesKit';

@CustomDialog
struct CustomDialogExample {
  @Consume('net') netState: string;
  controller?: CustomDialogController;

  build() {
    Column() {
      Text(this.netState)
        .fontSize(16)
        .textAlign(TextAlign.Center);

      Row() {
        Button('取消')
          .fontSize(16)
          .fontColor('#0A59F7')
          .backgroundColor(Color.Transparent)
          .width(120)
          .height(40)
          .borderRadius('50%');
        Button('确认')
          .fontSize(16)
          .backgroundColor('#0A59F7')
          .fontColor(Color.White)
          .width(120)
          .height(40)
          .borderRadius('50%');
      }
      .margin({ top: 20 })
      .width('100%')
      .justifyContent(FlexAlign.SpaceAround);
    }
    .padding(20);
  }
}

@Entry
@Component
struct NetworkDemo {
  @Provide('net') netState: string = '';
  @State change: boolean = false; <em>// 是否使用流量</em>
  @State @Watch('myShow') showDialog: boolean = false;
  private netCon: connection.NetConnection = connection.createNetConnection();
  dialogControllerConfirm: CustomDialogController = new CustomDialogController({
    builder: CustomDialogExample(),
    width: 300,
    cornerRadius: 40,
    onWillDisappear: () => {
      this.showDialog = false;
    }
  });

  myShow() {
    if (this.showDialog) {
      this.netState = '正在使用数据流量播放，请注意流量消耗。';
      this.dialogControllerConfirm.open();
    }
  }

  aboutToAppear(): void {
   <em> // 获取页面创建时的网络类型</em>
    connection.getDefaultNet()
      .then((netHandle: connection.NetHandle) => {
        if (netHandle.netId === 0) {
         <em> // 当前没有已连接的网络时，netHandle的netId为0，属于异常场景。</em>
          return;
        }
        connection.getNetCapabilities(netHandle, (error: BusinessError, data: connection.NetCapabilities) => {
          if (error) {
            return;
          }
          if (data.bearerTypes.length > 0) {
            let netType = data.bearerTypes[0]; <em>// 获取网络类型</em>
           <em> // 使用流量</em>
            if (netType === connection.NetBearType.BEARER_CELLULAR) {
              this.showDialog = true;
            } else {
              this.change = true;
            }
          }
        });
      })
      .catch((error: BusinessError) => {
        console.error(`Failed to get default net. Code:${error.code}, message:${error.message}`);
      });

    <em>// 监听网络类型切换</em>
    this.netCon.on('netCapabilitiesChange', (data: connection.NetCapabilityInfo) => {
      if (data.netCap.bearerTypes.length > 0) {
        let netType = data.netCap.bearerTypes[0];<em> // 获取当前网络类型</em>
        <em>// 当前网络为数据流量时进行提示</em>
        if (netType === connection.NetBearType.BEARER_CELLULAR && this.change) {
          this.showDialog = true;
        }
      }
    });

    <em>// 使用register接口注册网络状态变化事件。</em>
    this.netCon.register((error: BusinessError) => {
      console.error(JSON.stringify(error));
    });
  }


  aboutToDisappear(): void {
    this.netCon.unregister((error: BusinessError) => {
      console.error(JSON.stringify(error));
    });
  }

  build() {
    Column()
      .height('100%')
      .width('100%');
  }
}
```
